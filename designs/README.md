# designs/

Design artifacts (screenshots, mockups, and short design notes) that support implementation tickets.

Conventions:
- Prefer one subfolder per epic (e.g. `be-7hw/`).
- Name screenshots descriptively (e.g. `alternative2_plan_tab.png`).
- Include a short `design.md` in each folder with context + links to tickets.


