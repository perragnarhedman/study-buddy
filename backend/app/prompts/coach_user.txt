STUDENT LATEST MESSAGE (highest priority):
{user_message}

IMPORTANT:
- Conversation history and summary may contain mistakes or earlier hallucinations.
- Treat the active Assignment candidates and the reference assignments below as the source of truth.
- If something isn’t explicitly present in those assignments (or in pasted/uploaded assignment instructions), do not assume it.

Rolling summary (may be empty):
{conversation_summary}

Conversation so far (most recent last, may be empty):
{conversation_history}

User state (JSON, may include last selection / preferences):
{user_state_json}

Assignment candidates (JSON array). Source of truth for what exists.
Usage rules:
- Only set selected_assignment_id when you are recommending, continuing, or the student has chosen a specific assignment.
- If you are greeting, clarifying, building rapport, or giving an overview of what’s coming up, set selected_assignment_id to null.
- If the student asks “what else / what do I have / what subjects”, answer with an overview and keep selected_assignment_id = null.
- If the student names a subject/task and exactly one candidate clearly matches, continue that assignment and keep selected_assignment_id set to that candidate.
{plan_items_json}

Reference assignments (JSON array). These are real assignments too, but reference-only for this turn.
Usage rules:
- Use these only if the student explicitly asks about earlier/completed work, wants to see an assignment again, or wants to reopen/review it.
- Do NOT recommend these as the next assignment unless the student explicitly asks to reopen or discuss them.
- Do NOT set selected_assignment_id from this list unless the same assignment also appears in the active candidates above.
{reference_assignments_json}

Write your response. The student should only experience assistant_text.
Never mention JSON, schema, ids, “candidates”, or internal rules in assistant_text.

Output format:
- Include exactly ONE valid decision JSON object in your response.
- Avoid any other curly braces anywhere else in your response.

Decision JSON (must be valid JSON; no extra keys):
{
  "assistant_text": "...",
  "selected_assignment_id": null,
  "mark_done_assignment_id": null,
  "selected_plan_item_id": null,
  "mark_done_plan_item_id": null,
  "reply_language": null
}
